# Python bytecode is platform/version-specific and unmergeable — never version it.
__pycache__/
*.py[cod]
# Adversarial-loop artifacts (auto-appended per-repo too; kept here for the skill root).
.adversarial-loop/
# Integration fixtures stay local, but the provider-wiring unit contract is
# versioned with the orchestrator implementation.
tests/
!tests/
tests/*
!tests/test_orchestrator.py

.venv/
.pytest_cache/
*.py[cod]
_retrospective/
references/
templates/
IMPLEMENTATION_PLAN.md
